name: Flutter Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    # بہترین مطابقت کے لیے ubuntu-latest استعمال کریں
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    # Flutter ماحول کو سیٹ اپ کریں
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.x' # یہاں اپنے Flutter ورژن کو سیٹ کریں

    - name: Run flutter pub get
      run: flutter pub get
      # WORKING DIRECTORY کو پراجیکٹ کے اندرونی روٹ تک ایڈجسٹ کریں
      working-directory: ./DigitalWaqt_Azan_Full
      # 💥 فکس یہاں ہے: شیل کو /bin/sh پر سیٹ کریں
      shell: /bin/sh -c "{0}" 

    - name: Run Flutter build (Example)
      run: flutter build apk --release
      working-directory: ./DigitalWaqt_Azan_Full
      shell: /bin/sh -c "{0}"
      
    # ... (باقی بلڈ اور اپ لوڈ کے مراحل)
